---
import { constants } from '../constants';

---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <meta
      name="description"
      content=`Portfolio of ${constants.fullname} - ${constants.job.title} at ${constants.job.employer}`
    />
    <title>Portfolio of {constants.fullname}</title>
  </head>
  <body>
    <slot />
  </body>
</html>
